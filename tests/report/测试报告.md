# MiniSaaS ERP 质量测试报告（修复后回归版）

> 测试执行：2026-08-17 · 测试方式：真实 Chrome 浏览器 E2E + 接口全用例 + 安全专项 + 并发压测
> 状态：**15 项缺陷全部修复，四层回归 106/106 全绿**

---

## 一、测试总览（修复后）

| 测试层 | 用例数 | 通过 | 失败 | 通过率 | 观察项 |
| --- | --- | --- | --- | --- | --- |
| 接口层（API） | 56 | 56 | 0 | **100%** | 0 |
| 安全专项 | 23 | 23 | 0 | **100%** | 1 |
| UI E2E（Chrome 真实浏览器） | 21 | 21 | 0 | **100%** | 0 |
| 并发与健壮性 | 6 | 6 | 0 | **100%** | 2 |
| **合计** | **106** | **106** | **0** | **100%** | **3** |

> 对比修复前：97 用例 95 通过（97.9%），其中 2 个失败用例（U3 登录提示、S5-2 null body）均为真实缺陷，修复后转为通过；并新增 9 个针对修复点的断言用例（幂等键、负价、404、限流、CORS、413 等）。

---

## 二、修复清单（15/15 项全部完成）

### 🔴 P1 严重（2/2 已修复）

| # | 缺陷 | 修复方案 | 回归验证 |
| --- | --- | --- | --- |
| 1 | 可创建负价商品 | 新增 `validate.ts` 严格校验：`price` 必须为正数（≥0.01） | ✅ B9/B10 断言 400 通过 |
| 2 | 登录失败无错误提示 | axios 401 拦截器排除 `/auth/*` 接口，登录失败不再触发整页刷新 | ✅ U3 断言提示显示 + 无刷新通过 |

### 🟠 P2 中等（4/4 已修复）

| # | 缺陷 | 修复方案 | 回归验证 |
| --- | --- | --- | --- |
| 3 | 畸形 JSON 请求体 → 500 | 全局错误处理识别 body-parser 4xx（`err.type`）返回 400 | ✅ S5-2 断言 400 |
| 4 | 超大请求体 → 500 | 同上，413 按状态码返回 | ✅ S5-5 断言 413 |
| 5 | 删除不存在资源 → 500 | Prisma P2025 → 404（全局 + 路由双重处理） | ✅ B16/C6/S4-1 断言 404 |
| 6 | 非数字价格 → 500 | `toNumber` 严格类型校验（拒绝 NaN/字符串/布尔） | ✅ S5-3 断言 400 |

### 🟡 P3 建议（9/9 已修复）

| # | 建议 | 修复方案 | 回归验证 |
| --- | --- | --- | --- |
| 7 | 状态机流转顺序校验 | 后端 `STATUS_FLOW` 白名单，禁止跳级 | ✅ D13 断言 400 |
| 8 | 登录失败限流 | 内存限流：5 次失败锁定 15 分钟，429 响应（修复了计数逻辑 bug） | ✅ S3-4 断言 429 |
| 9 | X-Powered-By 泄露 | `app.disable('x-powered-by')` | ✅ S4-3 断言无此头 |
| 10 | CORS 全开放 | 白名单配置（`CORS_ORIGINS`，默认 localhost:5173） | ✅ S4-4 断言恶意来源无 ACAO |
| 11 | 布尔/数值类型宽松 | `toInt`/`toNumber` 严格校验 | ✅ S5-4 断言 400 |
| 12 | 字段长度无上限 | 用户名 3-50、密码 6-100、商品名 ≤100、SKU ≤50 等 | ✅ A10 断言 400 |
| 13 | 0 元商品 | `price ≥ 0.01` 校验 | ✅ B9 断言 400 |
| 14 | 无幂等键 | `Idempotency-Key` 请求头 + 数据库唯一列（持久化，并发安全） | ✅ D16/stress 断言同 key 只创建一单 |
| 15 | orderNo 撞号风险 | `Date.now() + randomUUID` 生成 | ✅ 并发 15 单唯一 |

---

## 三、核心能力验证（修复后仍全部通过）

- **事务防超卖**：10 并发抢 5 件 → 恰好成功 5、拒绝 5、0 服务器错误，库存不超卖
- **库存一致性**：下单-取消循环 5 轮精确回补；取消订单回补库存
- **金额防篡改**：客户端伪造 totalAmount/unitPrice 不生效，后端按数据库单价重算
- **安全**：SQL 注入 / JWT 篡改伪造 / 万能密码 / 路径遍历全部拦截；密码 bcrypt 哈希存储（直查库验证）；XSS 载体前端渲染为文本未执行（Chrome 实测）
- **看板统计口径**：已取消订单不计入营收

---

## 四、测试证据

- 各层原始结果：`tests/report/api-result.json`、`security-result.json`、`stress-result.json`、`ui-result.json`
- UI 截图：`tests/report/screenshots/`（14 张，登录/看板/CRUD/下单/明细/状态/退出）
- 回归用例：`tests/*.test.mjs`（`node api.test.mjs` 等，需先启动前后端）

## 五、遗留说明

- 3 个观察项均为**预期行为**：超卖并发精确拒绝、订单号并发唯一、XSS 载体入库（防护在前端渲染层）
- 限流为内存实现（进程重启失效），生产建议换 Redis；幂等键用数据库唯一列实现（重启持久）
- 测试产生的脏数据已清理，数据库已恢复种子基线（admin/admin123），端口已释放
