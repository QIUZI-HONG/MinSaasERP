# 面试讲解提纲（1 天速成版）

> 这份文档帮你把项目"讲明白"。核心原则：**只讲你亲手验证过的内容**，被问到不会的地方就大方说"这块我目前简化了，但我知道生产环境该怎么做"——这恰好符合岗位要的"诚实 + 质量兜底"人设。

---

## 一、开场白（30 秒版，背熟）

> "我用一天时间，借助 Cursor/Claude 从零搭了一个迷你 SaaS ERP 演示系统，用来证明我能驾驭 AI 完成全栈开发并为代码质量兜底。技术栈和咱们岗位完全一致：Node.js + TypeScript + Prisma 后端，Vue 3 + TypeScript + Vite 前端。核心模块有登录鉴权、商品、客户、订单（下单事务扣库存）和数据看板。我现在给您演示。"

**为什么这样说**：一句话同时回答了"你做了什么"“技术栈匹配”“和岗位的关联”。

---

## 二、两分钟演示讲解稿（照着念）

1. **登录页**："这是 JWT 鉴权。密码用 bcrypt 哈希存储，登录成功返回 token，前端 axios 拦截器自动带上 token，过期自动跳回登录页。"
2. **数据看板**："这里显示商品/客户/订单总数和累计营收。注意营收计算排除了已取消订单——这是我在 review AI 生成的代码时发现并修正的统计口径问题。"
3. **商品管理**："增删改查 + 按 SKU/名称搜索。删商品时如果被订单引用，后端会返回业务错误提示——这是外键保护。"
4. **订单管理（重点）**："新建订单选客户、选商品、填数量。提交时后端在一个**数据库事务**里做了三件事：校验库存、计算总金额、扣减库存——任何一步失败整个事务回滚。"
5. **状态流转**："订单可以变更状态。您看我把这个订单取消——取消后库存自动回补。这也是事务保证的，不会出现'订单取消了但库存没加回来'的脏数据。"
6. **收尾**："整个项目 5 张表、1 个事务扣库存的核心链路，我是分步指挥 AI 完成的：先定数据模型，再写接口，再写页面，每步都自己跑通验证。"

---

## 三、技术点逐个怎么讲

### 1. 为什么选这个技术栈

- **Prisma**：Schema 即数据库文档，类型安全，改模型后 `prisma db push` 自动同步表结构
- **SQLite**：演示零配置；生产换 PostgreSQL 只改一行配置——**这体现"知道生产环境怎么做"**
- **Vue 3 + Vite**：和岗位完全一致；Vite 开发热更新快

### 2. JWT 鉴权流程（画在纸上讲）

```
登录 → 后端校验密码 → 签发 token（含 userId/username/role，7 天有效）
前端存 localStorage → 每次请求 axios 拦截器加 Authorization: Bearer token
后端 requireAuth 中间件校验 → 失败返回 401 → 前端拦截器跳回登录页
```

### 3. 事务扣库存（项目灵魂，必须会讲）

```
下单 = 校验库存(够吗) → 计算金额 → 扣库存 → 写订单+明细
      └────────── 全部包在 $transaction 里 ──────────┘
```

"如果不放事务：订单写成功了但扣库存那行失败，库存就对不上了。事务保证要么全成功、要么全回滚。"

### 4. 数据模型设计

- 订单明细单独一张表：一个订单多商品，不能把商品塞进订单字段里（违反第一范式）
- 明细里存 `unitPrice` 快照：商品改价不影响历史订单金额
- 金额计算放后端：前端显示的价格只是预览，可信金额以后端为准

### 5. 前端结构

- axios 封装：统一 baseURL、请求带 token、响应拦截 401
- 路由守卫：`beforeEach` 里判断未登录跳 `/login`
- 订单状态字典 `constants.ts`：状态码和显示文案集中管理，避免魔法字符串

---

## 四、AI 协作记录（面试官极可能问："这个项目 AI 参与了多少？"）

**诚实回答**："项目 90% 的代码由 AI 生成，但每一步我都审查并亲自验证。关键修正有三个："

| #   | AI 最初怎么写的                 | 我发现了什么问题                 | 我怎么改的                                     |
| --- | ------------------------------- | -------------------------------- | ---------------------------------------------- |
| 1   | 统计营收直接 `sum(totalAmount)` | 把已取消订单也算进去了，口径错误 | 加上 `where: { status: { not: 'CANCELLED' } }` |
| 2   | 下单扣库存不在事务里            | 扣库存和写订单可能不一致         | 用 `prisma.$transaction` 包裹                  |
| 3   | 删商品直接 `delete`             | 被订单引用的商品删了会报 500     | 捕获外键错误 P2003，返回友好业务提示           |

**加分句**："这正好对应岗位要求的'审查和纠正 AI 错误产出的能力'——AI 出代码，我为质量兜底。"

---

## 五、面试官可能问的问题 + 参考答案（新手版）

**Q1：什么是事务？为什么这里要用？**
A：事务是一组要么全部成功、要么全部回滚的数据库操作。下单要同时"扣库存+写订单"，中间任何一步失败都要撤销，否则数据不一致。

**Q2：JWT 和 Session 有什么区别？**
A：Session 存在服务器内存/Redis，客户端只拿一个 id；JWT 是签名后的 JSON 自包含，服务器不用存状态，适合无状态 API。缺点是无法主动失效，所以设置 7 天过期。（能答到这个程度对新手够用）

**Q3：为什么密码不存明文？**
A：数据库泄露时明文密码直接暴露，且用户可能在多个网站用同一个密码。bcrypt 是单向哈希 + 加盐，即使拿到哈希也几乎无法反推。

**Q4：SQLite 能用于生产吗？**
A：演示可以；生产高并发时会有写锁竞争。换 PostgreSQL/MySQL 只需改 Prisma 的 datasource provider 和连接串，模型代码不用动——这就是 ORM 的好处。

**Q5：如何防止越权？**
A：本项目每个接口都挂 `requireAuth` 中间件，先验 token 再进路由。更细的权限（比如销售只能看自己的订单）可以在中间件里加 role 判断，这是已知的扩展点。

**Q6：为什么前端也显示金额，信任哪个？**
A：前端合计只是预览，方便用户确认；真正金额由后端根据数据库里的单价计算，前端传的金额后端忽略。防止有人改请求伪造价格。

**Q7：这个项目如果上线，还缺什么？**
A：分页、细粒度权限、输入校验库（zod）、单元/集成测试、HTTPS、日志监控、数据库迁移流程（prisma migrate）、部署（Docker）。架构上已经留了扩展点。

**Q8：Vue 里 v-model 和 :value + @change 有什么区别？（考 Vue 基础）**
A：v-model 是语法糖，等价于 `:value` + `@input`（表单元素），双向绑定。单向数据流原则下更推荐显式写 props + events。

**Q9：axios 拦截器是干什么的？**
A：请求拦截统一加 token，响应拦截统一处理 401 跳登录、以及把错误提取成可读 message，页面里就不用每个请求都写重复的鉴权逻辑。

**Q10：为什么订单明细要单独一张表？**
A：一对多关系必须拆表：一个订单对应多行商品，每行有自己的数量单价。如果塞进订单表一个字段，要么存 JSON 要么违反范式，都不利于统计和联查。

---

## 六、演示脚本（面试现场版）

```
1. 打开 http://localhost:5173 → 登录页 → 输入 admin/admin123 → 登录
2. 停在"数据看板"，讲统计卡片 + 最近订单
3. 点"商品管理" → 搜索"键盘" → 展示搜索结果 → 清空 → 新增一个商品 → 保存
4. 点"订单管理" → 新建订单 → 选客户 → 加两行明细 → 提交 → 提示"下单成功，库存已扣减"
   （此时可切回商品管理展示该商品库存减少了——这是"全链路"的证据）
5. 回订单管理 → 展开订单看明细 → 变更状态"已发货"
6. 点右上角退出登录 → 再登录一次（演示 token 流程）
7. 结束语："以上就是完整闭环。细节我可以继续展开。"
```

> 技巧：第 4 步**先记住某商品库存数**，下单后再切回去对比，现场效果非常直观。

---

## 七、今晚/明天必做的 5 件事（按优先级）

1. **亲手跑通项目**：按 README 启动前后端，完整走一遍演示脚本（约 30 分钟）
2. **改一个 bug / 加一个小功能**：比如把看板改成按月份显示营收、或商品列表加分页。亲自动手改过的代码，被问细节才不慌
3. **通读一遍 `server/src/routes/orders.ts`**：这是面试主战场，弄懂事务那段（不懂就问 AI，让它逐行解释）
4. **用 AI 把整份代码 review 一遍**：把代码丢给 Claude 让它找问题——面试官问"你怎么用 AI 审查代码"时，你就有真实案例了
5. **对着镜子练一遍上面的演示稿**：控制在 2 分钟内

---

## 八、心态提示

- 你不需要懂所有原理——面试官知道你是新人，考察的是**学习能力和 AI 协作能力**，这两点这个项目就是证据
- 遇到不会的问题先说思路，再诚实说"目前没深入，但我知道从哪查"
- 岗位要的就是"能指挥 AI + 能兜底质量"，你带着一个 AI 协作完成的完整项目去，已经比大多数候选人契合了
